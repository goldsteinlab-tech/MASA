% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readNucleotideCodeForChromosome.R
\name{readNucleotideCodeForChromosome}
\alias{readNucleotideCodeForChromosome}
\title{Read or Generate Nucleotide Codes for a Chromosome}
\usage{
readNucleotideCodeForChromosome(
  chr,
  nmer = 4,
  genome = NA,
  nuccodefileDir = ".",
  mapdir = ""
)
}
\arguments{
\item{chr}{Character. Chromosome name or identifier.}

\item{nmer}{Integer. Length of the nucleotide sequence to consider (default is 4).}

\item{genome}{Named list or object. Genome sequence containing chromosome data.}

\item{nuccodefileDir}{Character. Directory where nucleotide code files are stored (default is current directory).}

\item{mapdir}{Character. Directory where mappability files are stored (default is empty string).}
}
\value{
List with two elements:
\item{code}{Integer vector of nucleotide codes for the chromosome.}
\item{freqtable}{Data frame with columns \code{seq} (nucleotide sequence label) and \code{count} (frequency of each code).}
The result is also saved as an RDS file in \code{nuccodefileDir}.
}
\description{
Reads or computes nucleotide codes for a specified chromosome. If a precomputed code file exists, it loads the codes from disk; otherwise, it computes the codes from the genome sequence, optionally masking unmappable bases. Returns a list containing the nucleotide codes and a frequency table.
}
\examples{
# Example usage:
# genome <- list(chr1 = "ACGTACGTACGT")
# result <- readNucleotideCodeForChromosome("chr1", nmer = 4, genome = genome)
}
