% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/homer.R
\name{run_homer}
\alias{run_homer}
\title{Run HOMER Motif Analysis}
\usage{
run_homer(
  input_file,
  output_dir,
  genome = "mm10",
  homer_path = "findMotifsGenome.pl",
  additional_args = NULL,
  process_results = TRUE
)
}
\arguments{
\item{input_file}{Character. Path to the input peak file (BED format).}

\item{output_dir}{Character. Directory to save HOMER results (will be created if needed).}

\item{genome}{Character. Genome identifier (e.g., "hg38", "mm10"). Default is "mm10".}

\item{homer_path}{Character. Path to HOMER's findMotifsGenome.pl script (default: "findMotifsGenome.pl" in PATH).}

\item{additional_args}{Character. Additional arguments for HOMER as a single string (e.g., "-len 10 -size 200").}

\item{process_results}{Logical. If TRUE, process and return results as R objects. Default is TRUE.}
}
\value{
If \code{process_results} is TRUE, returns a list of processed HOMER results; otherwise, returns NULL invisibly.
}
\description{
Runs HOMER's motif discovery tool (\code{findMotifsGenome.pl}) on a set of peaks and optionally processes the results.
}
\examples{
# run_homer("peaks.bed", "homer_output", genome = "hg38", additional_args = "-len 10 -size 200")
}
